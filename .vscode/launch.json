{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "ng serve (local)",
      "type": "msedge",
      "request": "launch",
      // "preLaunchTask": "npm: start-dev",
      "url": "http://localhost:4200/",
      "webRoot": "${workspaceFolder}"
    },
    {
      "name": "ng serve (prod)",
      "type": "msedge",
      "request": "launch",
      // "preLaunchTask": "npm: start",
      "url": "http://localhost:4200/",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
